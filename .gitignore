# Pasta onde fica guardada a base de dados (não deve ser incluída no repositório)
db-data/

# Pasta onde ficam guardados os ficheiros do WordPress (também não deve ir para o repositório)
wp-data/

# Ficheiro que contém passwords e outras credenciais reais (manter fora do repositório)
.env

# Ficheiros e lixo comuns que não interessam para versionamento
.DS_Store
*.log